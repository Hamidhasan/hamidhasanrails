<%= form_for(@passenger, html: {class: "nice"}) do |f| %>
  <% if @passenger.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@passenger.errors.count, "error") %> prohibited this passenger from being saved:</h2>

      <ul>
      <% @passenger.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name, class: 'input-text' %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email, class: 'input-text' %>
  </div>

  <div class="actions">
    <%= f.submit class: 'medium nice blue radius button' %>
  </div>
<% end %>
