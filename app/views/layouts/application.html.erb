<!DOCTYPE html>
<html>
<head>
  <title>Travel Recorder</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class = "container"> 
    <% if passenger_signed_in? %>
       <div class="row">
          <div class="ten columns">
            <%= yield %>
          </div>
          <div class="two columns">
            <br />
            <div id="hidetext">
              <button class="small nice white radius button">Toggle</button>
            </div>
            <br />
            <div id="sidebar">
              <%= button_to 'Create New Flight', new_flight_path, 
              class: 'small nice blue radius button', :method => :get %>
            <%= "Logged in as: "%><br />
            <%= link_to current_passenger.name, current_passenger %><br />
            <%= link_to 'Logout', destroy_passenger_session_path, method: :delete, class: 'small nice red radius button' %>
            </div>
          </div>
    <% else %>
     <%= yield %>
    <% end %>
    </div>
</div>

</body>
</html>
